# EDA AI Lab

Experiments combining AI with EDA and VLSI workflows.

## First experiment

Call an LLM from Python and save its response.

## Setup

1. Create and activate a Python virtual environment.
2. Install dependencies.
3. Add `OPENAI_API_KEY` to `.env`.
4. Run `python src/hello_llm.py`.
